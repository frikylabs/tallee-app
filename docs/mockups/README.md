# Design mockups

Exported stills and short recordings of the interactive prototypes that informed the locked design spec
in [`../design.md`](../design.md) — the hero-board direction gallery, the composed "flow v2", and the
two-phone storyboard for the open.

The prose spec is the contract; these are the visual reference for it. Files land here as the prototypes
are captured to static assets.
